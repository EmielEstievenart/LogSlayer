{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LogSlayer",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.buildDirectory}/bin/LogSlayer.exe",
            "args": [],
            "cwd": "${command:cmake.buildDirectory}",
            "externalConsole": true,
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
